name: wasalni_app
description: "تطبيق Wasalni للكابتن"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  google_maps_flutter: ^2.14.0
  geolocator: ^10.1.1
  image_picker: ^1.2.1
  provider: ^6.1.5+1
  intl: ^0.20.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
